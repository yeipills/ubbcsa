<div class="min-h-screen bg-gray-900">
  <%= content_for :navbar do %>
    <%= render 'shared/navbar' %>
  <% end %>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <% if flash[:alert] %>
      <div class="bg-red-900/50 p-4 rounded-lg border border-red-800/50 mb-6">
        <p class="text-red-300 text-sm"><%= flash[:alert] %></p>
      </div>
    <% end %>
    
    <!-- Mensaje informativo sobre requisitos previos -->
    <div class="bg-blue-900/30 p-4 rounded-lg border border-blue-800/30 mb-6">
      <h3 class="text-blue-300 font-medium text-sm mb-1">Información importante</h3>
      <p class="text-blue-200 text-sm">
        Para crear un quiz, asegúrese de que:
      </p>
      <ul class="list-disc list-inside text-blue-200 text-sm mt-1 space-y-1">
        <li>El curso tenga al menos un laboratorio asociado</li>
        <li>Seleccione un laboratorio para el quiz (campo obligatorio)</li>
        <li>Establezca las fechas de inicio y fin si desea publicarlo automáticamente</li>
      </ul>
    </div>

    <div class="bg-gray-800 rounded-lg p-6">
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold text-white">Nuevo Quiz</h1>
        <%= link_to "Crear un laboratorio primero", new_curso_laboratorio_path(@curso), class: "text-sm text-blue-400 hover:text-blue-300" if @laboratorios.empty? %>
      </div>

      <% if @laboratorios.empty? %>
        <div class="bg-yellow-900/30 p-4 rounded-lg border border-yellow-800/30 mb-6">
          <p class="text-yellow-300 text-sm">
            <strong>Advertencia:</strong> Este curso no tiene laboratorios asociados. Debe crear al menos un laboratorio antes de poder crear un quiz.
          </p>
        </div>
      <% end %>

      <%= form_with(model: [@curso, @quiz], class: "space-y-6") do |f| %>
        <% if @quiz.errors.any? %>
          <div class="bg-red-900/50 p-4 rounded-lg border border-red-800/50 mb-6">
            <h2 class="text-red-300 font-medium mb-2 text-sm">
              <%= pluralize(@quiz.errors.count, "error") %> impidieron guardar este quiz:
            </h2>
            <ul class="list-disc list-inside text-red-300 text-sm space-y-1">
              <% @quiz.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="grid grid-cols-1 gap-6">
          <div>
            <%= f.label :titulo, class: "block text-sm font-medium text-gray-300" %>
            <%= f.text_field :titulo, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
          </div>

          <div>
            <%= f.label :descripcion, class: "block text-sm font-medium text-gray-300" %>
            <%= f.text_area :descripcion, rows: 3, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
          </div>

          <div>
            <%= f.label :laboratorio_id, "Laboratorio", class: "block text-sm font-medium text-gray-300" %>
            <div class="flex items-center">
              <%= f.collection_select :laboratorio_id, @laboratorios, :id, :nombre, 
                  { prompt: "Seleccione un laboratorio" }, 
                  class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
              <span class="ml-2 text-red-500">*</span>
            </div>
            <p class="mt-1 text-xs text-gray-400">Campo obligatorio. Si no hay laboratorios disponibles, debe crear uno primero.</p>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
            <div>
              <%= f.label :tiempo_limite, "Tiempo límite (minutos)", class: "block text-sm font-medium text-gray-300" %>
              <%= f.number_field :tiempo_limite, min: 1, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
            </div>

            <div>
              <%= f.label :intentos_permitidos, class: "block text-sm font-medium text-gray-300" %>
              <%= f.number_field :intentos_permitidos, min: 1, class: "mt-1 block w-full rounded-md border-gray-600 bg-gray-700 text-white shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
            </div>
          </div>
        </div>

        <div class="flex justify-end space-x-3 mt-6">
          <%= link_to "Cancelar", curso_path(@curso), class: "px-4 py-2 text-sm font-medium text-gray-300 bg-gray-700 rounded-md hover:bg-gray-600" %>
          <%= f.submit "Crear Quiz", class: "px-4 py-2 text-sm font-medium text-white bg-blue-600 rounded-md hover:bg-blue-500" %>
        </div>
      <% end %>
    </div>
  </div>
</div>