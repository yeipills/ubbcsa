<!-- app/views/admin/shared/_sidebar.html.erb -->
<div class="fixed left-0 top-0 w-64 h-full bg-gray-900 text-white p-4">
  <div class="mb-8">
    <h2 class="text-2xl font-bold">UBBCSA Admin</h2>
  </div>

  <nav class="space-y-2">
    <%= link_to admin_root_path,
        class: "flex items-center space-x-2 p-3 rounded hover:bg-gray-800 #{current_page?(admin_root_path) ? 'bg-gray-800' : ''}" do %>
      <i class="fas fa-home"></i>
      <span>Dashboard</span>
    <% end %>

    <%= link_to admin_usuarios_path,
        class: "flex items-center space-x-2 p-3 rounded hover:bg-gray-800" do %>
      <i class="fas fa-users"></i>
      <span>Usuarios</span>
    <% end %>

    <%= link_to admin_laboratorios_path,
        class: "flex items-center space-x-2 p-3 rounded hover:bg-gray-800" do %>
      <i class="fas fa-flask"></i>
      <span>Laboratorios</span>
    <% end %>

    <%= link_to admin_reportes_path,
        class: "flex items-center space-x-2 p-3 rounded hover:bg-gray-800" do %>
      <i class="fas fa-chart-bar"></i>
      <span>Reportes</span>
    <% end %>

    <%= link_to admin_configuracion_path,
        class: "flex items-center space-x-2 p-3 rounded hover:bg-gray-800" do %>
      <i class="fas fa-cog"></i>
      <span>Configuración</span>
    <% end %>
  </nav>
</div>